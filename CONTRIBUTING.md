# Contributing

Behavior definitions live in data/behaviors.json. Keep task definitions reproducible and implementation-agnostic. Never present illustrative baseline metrics as measured hardware results.

Run locally with npm start. API endpoints: GET /api/health, GET /api/behaviors, GET /api/behaviors/:id, POST /api/benchmark.