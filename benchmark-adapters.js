const adapters={"spec-validator":async({input})=>({adapter:"spec-validator",kind:"software",status:"passed",note:"Validates task specification only; not physical robot performance.",stepsChecked:Array.isArray(input.completedSteps)?input.completedSteps.length:0}),"mock-sim":async()=>({adapter:"mock-sim",kind:"simulation-placeholder",status:"available",note:"Adapter contract only. Connect a real simulator before publishing simulation measurements.",environment:"not-configured"})};async function runAdapter(name,args){if(!adapters[name])throw new Error("Unknown adapter: "+name);return adapters[name](args)}module.exports={adapters,runAdapter};